language: go
versions:
  - '1.20'

steps:
  - name: Install courtney
    command: make setupenv

  - name: Build
    command: make build

  - name: Test & coverage
    command: echo $PATH && make cov
    envvars:
      PATH: "$PATH:$HOME/go/bin:$HOME/usr/bin/go"
