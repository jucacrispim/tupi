language: go
versions:
  - '1.20'

envvars:
  PATH: "$PATH:$HOME/go/bin"
  GOPATH: $HOME/go

steps:
  - name: Install courtney
    command: make setupenv

  - name: Build
    command: make build

  - name: Test & coverage
    command: echo $PATH && make cov
